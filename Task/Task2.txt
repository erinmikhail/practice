Даня 
Задача: Исправить баг с текстовым импортом (ответ GigaChat)
В чем проблема: 
Сейчас импорт чеков работает отлично, но при вводе текста выдает ошибку сервера. 
Это происходит в файле text_AI.py. 
Нейросеть иногда отвечает не чистым JSON-массивом, а добавляет лишний текст, из-за чего ломается парсер json.loads.

Что нужно сделать:
Доработать системный промпт в text_AI.py, чтобы GigaChat на 100% выдавал строгий формат.
Сделать более умную очистку текста в функции clean_json_response (чтобы она находила квадратные скобки [ и ] даже в окружении мусора).
Настроить повторную попытку (try/except): если GigaChat выдал кривой JSON, скрипт должен не просто падать, а отправлять запрос к ИИ еще раз (максимум 2-3 попытки).