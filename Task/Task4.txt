Полносью кратко:
Прием файлов: Написать эндпоинт POST /api/ai/parse (в файле api/endpoints/ai.py), который умеет принимать UploadFile от фронтенда (чеки, скриншоты).
Валидация файлов: Проверить, что прислали именно картинку (jpeg/png/pdf), а не бинарник вируса, и что файл весит не больше 5-10 МБ.
Связка с AI: Вызвать функцию (Task2), передав ей картинку, и дождаться ответа.
Массовое сохранение: Написать эндпоинт POST /transactions/batch, который принимает массив подтвержденных транзакций из черновиков и передает их в твою функцию массовой вставки (bulk_save_objects) в базу данных.

К утру пятницы: 
Эндпоинт принимает один скриншот (PNG/JPG), передает Task2 и возвращает черновики.